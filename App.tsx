/**
 * This file was at the repository root and triggered:
 * "Cannot find module 'react' or its corresponding type declarations."
 * The project already contains a configured React app under frontend/.
 * To resolve the error in the root TypeScript context (which is not a React app),
 * replace this file with a tiny placeholder so it no longer imports React or declares a component.
 * The real React App lives in frontend/src/App.tsx.
 */

// Intentionally empty module to satisfy TypeScript at the repository root.
export {};
